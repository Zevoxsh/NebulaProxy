# Dependencies
node_modules/
*/node_modules/

# Environment variables
.env
.env.local
.env.production
.setup-required

# Build output
dist/
build/
*/dist/
*/build/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Cache
.cache/
.parcel-cache/

# Database files
*.db
*.db-shm
*.db-wal
database.db

# SSL Certificates
*.pem
*.key
*.crt
*.csr
/etc/letsencrypt/
certs/

# Backups
backups/
*.tar.gz
*.zip
*.backup
*.old
*.bak

# Security secrets (NEVER commit these!)
secrets-*.txt
.env.secrets
*-secrets.txt
*.secret

# Docker volumes data
data/

# Temporary files
tmp/
temp/
*.tmp

# Test coverage
coverage/
.nyc_output/
*.lcov
.claude/worktrees/

# Documentation site (standalone, not part of the project)
docs-site/

# os/build/ holds the ISO-builder script, not JS build output — the
# */build/ rule above matches it by coincidence of name, so un-ignore the
# script explicitly while still excluding what it generates.
!os/build/
!os/build/*.sh
os/build/.cache/
os/build/*.iso
